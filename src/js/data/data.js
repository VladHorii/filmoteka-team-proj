// function createYear(obj) {
//   return obj.release_date ? obj.release_date.split('-')[0] : '';
// }

// export { createYear };

// Свойство с жанрами на 3 елемента
// function cutGenresListForHero(array, genres) {
//   return array
//     .map(id => genres.filter(element => element.id === id))
//     .slice(0, 3)
//     .flat();
// }

// export { cutGenresListForHero };
